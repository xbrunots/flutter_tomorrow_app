import 'package:flutter/widgets.dart';
import 'package:suamusica_weather/app/design_system/typography/ds_text_style.dart';

final class DSText {
  static DSTextStyle get bodySuperExtraLargeBold => const DSTextStyle(
        fontSize: 56,
        letterSpacing: 0,
        height: 1.5,
        fontWeight: FontWeight.bold,
      );

  static DSTextStyle get bodySuperExtraLarge => const DSTextStyle(
        fontSize: 56,
        letterSpacing: 0,
        height: 1.5,
        fontWeight: FontWeight.normal,
      );

  static DSTextStyle get bodyExtraLarge => const DSTextStyle(
        fontSize: 32,
        letterSpacing: 0,
        height: 1.5,
        fontWeight: FontWeight.normal,
      );

  static DSTextStyle get bodyExtraLargeBold => const DSTextStyle(
        fontSize: 32,
        letterSpacing: 0,
        height: 1.5,
        fontWeight: FontWeight.bold,
      );

  static DSTextStyle get bodyPlusLargeBold => const DSTextStyle(
        fontSize: 24,
        letterSpacing: 0,
        height: 1.5,
        fontWeight: FontWeight.bold,
      );

  static DSTextStyle get bodyPlusLarge => const DSTextStyle(
        fontSize: 24,
        letterSpacing: 0,
        height: 1.5,
      );

  static DSTextStyle get bodyLargeBold => const DSTextStyle(
        fontSize: 16,
        letterSpacing: 0,
        height: 1.5,
        fontWeight: FontWeight.bold,
      );

  static DSTextStyle get bodyLarge => const DSTextStyle(
        fontSize: 16,
        letterSpacing: 0,
        height: 1.5,
      );

  static DSTextStyle get bodyMedium => const DSTextStyle(
        fontSize: 14,
        letterSpacing: 0,
        height: 1.33,
      );

  static DSTextStyle get bodySmall => const DSTextStyle(
        fontSize: 12,
        letterSpacing: 0.5,
        height: 1.5,
      );

  static DSTextStyle get displayExtraLarge => const DSTextStyle(
        fontSize: 40,
        letterSpacing: -1,
        height: 1.2,
      );

  static DSTextStyle get displayLarge => const DSTextStyle(
        fontSize: 32,
        letterSpacing: -1,
        height: 1.2,
      );

  static DSTextStyle get displayMedium => const DSTextStyle(
        fontSize: 28,
        letterSpacing: -1,
        height: 1.2,
      );

  static DSTextStyle get displaySmall => const DSTextStyle(
        fontSize: 24,
        letterSpacing: -1,
        height: 1.2,
      );

  static DSTextStyle get headlineLarge => const DSTextStyle(
        fontSize: 20,
        letterSpacing: -0.5,
        height: 1.15,
      );

  static DSTextStyle get headlineMedium => const DSTextStyle(
        fontSize: 16,
        letterSpacing: -0.5,
        height: 1.15,
      );

  static DSTextStyle get labelPlusLargeBold => const DSTextStyle(
        fontSize: 20,
        letterSpacing: 0,
        height: 1.5,
        fontWeight: FontWeight.bold,
      );

  static DSTextStyle get labelPlusLarge => const DSTextStyle(
        fontSize: 20,
        letterSpacing: 0,
        height: 1.5,
      );

  static DSTextStyle get labelLarge => const DSTextStyle(
        fontSize: 16,
        letterSpacing: 0,
        height: 1.15,
      );

  static DSTextStyle get labelMedium => const DSTextStyle(
        fontSize: 12,
        letterSpacing: 0.5,
        height: 1.15,
      );

  static DSTextStyle get labelMediumBold => const DSTextStyle(
        fontSize: 12,
        letterSpacing: 0.5,
        height: 1.15,
        fontWeight: FontWeight.bold,
      );

  static DSTextStyle get labelSmall => const DSTextStyle(
        fontSize: 10,
        letterSpacing: 0.5,
        height: 1.5,
      );

  static DSTextStyle get titleLarge => const DSTextStyle(
        fontSize: 16,
        letterSpacing: 0,
        height: 1.15,
      );

  static DSTextStyle get titleLargeBold => const DSTextStyle(
    fontSize: 16,
    letterSpacing: 0,
    height: 1.15,
    fontWeight: FontWeight.bold,
  );

  static DSTextStyle get titleMedium => const DSTextStyle(
        fontSize: 14,
        letterSpacing: 0,
        height: 1.15,
      );

  static DSTextStyle get titleMediumBold => const DSTextStyle(
        fontSize: 14,
        letterSpacing: 0,
        height: 1.15,
        fontWeight: FontWeight.bold,
      );

  static DSTextStyle get input1 => const DSTextStyle(
        fontSize: 40,
        letterSpacing: -1,
        height: 1.15,
      );

  static DSTextStyle get input2 => const DSTextStyle(
        fontSize: 32,
        letterSpacing: -1,
        height: 1.15,
      );

  static DSTextStyle get input3 => const DSTextStyle(
        fontSize: 20,
        letterSpacing: -1,
        height: 1.15,
      );

  static DSTextStyle get input4 => const DSTextStyle(
        fontSize: 16,
        letterSpacing: 0,
        height: 1.15,
      );

  static DSTextStyle get input5 => const DSTextStyle(
        fontSize: 12,
        letterSpacing: 0.5,
        height: 1,
      );
}
